---
name: design
description: Фаза 2 инженерного процесса TeaShop — проектирование архитектуры фичи. Используй после research для создания диаграмм и архитектурных решений.
disable-model-invocation: true
---

## МОДЕЛЬ И НАСТРОЙКИ

**Перед началом работы сообщи разработчику:**

> Фаза DESIGN. Рекомендуемая модель: **claude-opus-4-6**
> (нужна глубокая аналитика — архитектурные решения, trade-offs, ADR).
> Текущая модель видна в строке статуса. Переключить: `/model opus`
> Подтверди, что готов начать, или укажи другую модель.

Не начинай работу до подтверждения разработчика.

---

## Промпт для Claude

ТЫ НАХОДИШЬСЯ В ФАЗЕ DESIGN.

ЗАДАЧА: $ARGUMENTS

СТЕК: HTML5 + CSS3 + JS ES6+ + Supabase (PostgreSQL, Auth, Storage)
АРХИТЕКТУРА: JAMstack, без бэкенда, RLS для безопасности

ПРОЧИТАЙ:
- Соответствующий research-документ из `docs/research/`
- `CLAUDE.md` для стандартов проекта

СОЗДАЙ файл `docs/design/design_[название-задачи].md` со следующим содержимым:

1. Диаграмма компонентов (Mermaid) — какие JS-модули участвуют, как связаны
2. Data flow диаграмма (Mermaid) — откуда данные → куда идут → что возвращается
3. Sequence диаграмма (Mermaid) — пользователь → JS → Supabase
4. Изменения в схеме БД (если нужны новые таблицы/поля/RLS-политики)
5. ADR (Architecture Decision Record) — если есть выбор между подходами

ПРОВЕРЬ СЕБЯ ПЕРЕД СОХРАНЕНИЕМ:
- Не нарушена ли модульность (catalog, constructor, cart, auth)?
- Учтены ли RLS-политики для новых таблиц?
- Не добавляется ли серверная логика?
- Логична ли sequence-диаграмма?

НЕ ПИШИ КОД. ТОЛЬКО АРХИТЕКТУРА И ДИАГРАММЫ.

После завершения — покажи путь к файлу и попроси разработчика подтвердить
дизайн перед переходом к PLAN.