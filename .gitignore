# Если в будущем понадобится скрыть ключи — переименуй config.js в config.example.js
# и создай локальный config.js (не в репозитории)
# Пока anon key публичный — можно оставить в репозитории

# Системные файлы
.DS_Store
Thumbs.db

# Редакторы
.vscode/settings.json
*.suo
*.user
